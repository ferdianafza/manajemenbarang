<%= form_with(model: barang) do |form| %>
  <% if barang.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(barang.errors.count, "error") %> prohibited this barang from being saved:</h2>

      <ul>
        <% barang.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :kode_barang, style: "display: block" %>
    <%= form.text_field :kode_barang %>
  </div>

  <div>
    <%= form.label :nama_barang, style: "display: block" %>
    <%= form.text_field :nama_barang %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
